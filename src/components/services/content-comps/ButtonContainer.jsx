import { Button } from "@/components/shared"

const ButtonContainer = () => {
  return (
    <div>
      <Button className="bg-black tracking-[2.5px] hover:border-black active:bg-black">
        BOOK A TABLE
      </Button>
    </div>
  );
}

export default ButtonContainer